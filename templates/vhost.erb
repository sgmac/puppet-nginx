server {

      listen <%= port %>;
      server_name <% server_name %>;
      root <%= root %>;

      access_log <%= logdir %>/<%= host %>_access.log;
      error_log  <%= logdir %>/<%= host %>_error.log;

}
